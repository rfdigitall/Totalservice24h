{
  "name": "totalservice24h.it",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:city-pages": "node scripts/generate-city-pages.mjs",
    "generate:city-pages:idraulico": "node scripts/generate-city-pages-idraulico.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "patch:perf": "node scripts/patch-html-perf.mjs",
    "audit:pages": "node scripts/audit-pages.mjs",
    "publish": "node scripts/publish-dist.mjs",
    "deploy": "npm run build && npm run publish"
  },
  "dependencies": {
    "framer-motion": "^12.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "sharp": "^0.35.3",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0"
  }
}
